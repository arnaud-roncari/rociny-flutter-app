import 'package:flutter/material.dart';

class ProductPlacementForm extends StatelessWidget {
  const ProductPlacementForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
